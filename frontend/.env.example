# Vercel — required for API proxy (no trailing slash, no /api)
RENDER_API_URL=https://your-app.onrender.com

# Optional: call Render directly instead of /api proxy
# VITE_API_URL=https://your-app.onrender.com/api
